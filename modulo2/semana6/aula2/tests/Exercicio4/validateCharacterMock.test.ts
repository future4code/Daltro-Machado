// Letra b

test("Creating Mocks", () => {
    const validatorMock = jest.fn(() => {
			return true
		})
})

// Letra c

test("Creating Mocks", () => {
    const validatorMock = jest.fn(() => {
			return false
		})
})